# TODO

- [ ] Update favicon to match logo (use `public/logo.png` as source)
  - [ ] Regenerate `public/favicon.ico` (and other sizes) from logo
  - [x] Update `index.html` with `<link rel="icon">` + apple-touch icon references
  - [ ] Test in browser (hard refresh) to confirm tab/favicon updates

